{
  "log-driver": "{{ docker_daemon_config.log_driver }}",
  "log-opts": {
    "max-size": "{{ docker_daemon_config.log_opts.max_size }}",
    "max-file": "{{ docker_daemon_config.log_opts.max_file }}"
  }
}